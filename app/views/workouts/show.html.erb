<%#= turbo_frame_tag dom_id(@workout) do %>
  <div class="flex justify-between">
    <h1 class="text-2xl font-bold truncate">
      <%= link_to "Workouts ‹", root_path, data: { turbo_frame: "_top" }, class: "p-2 rounded hover:bg-gray-200" %>
      <%= @workout.category %>
      created <%= time_ago_in_words(@workout.created_at) %> ago
    </h1>
  </div>

  <div class="mt-4">
    <h4 class="font-medium text-gray-700 text-sm">Blocks</h4>
    <div id="workouts" class="flex flex-col divide-y divide-gray-200">
      <%= render @workout.blocks.order(created_at: :desc) %>
    </div>
  </div>
<%# end %>

